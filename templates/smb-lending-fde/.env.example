# Pi reads this only in the server process. Never expose it to browser code.
__SECRET_REF__=
# A live model is local-only and disabled unless this exact value is set.
NODEKIT_ENABLE_LOCAL_LIVE_PI=false
PORT=4173
